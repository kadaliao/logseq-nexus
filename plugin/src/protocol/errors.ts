export type ErrorCode = 'INVALID_INPUT' | 'UNAUTHORIZED' | 'INTERNAL';

export const ErrorCodes = {
  InvalidInput: 'INVALID_INPUT' as ErrorCode,
  Unauthorized: 'UNAUTHORIZED' as ErrorCode,
  Internal: 'INTERNAL' as ErrorCode,
};
